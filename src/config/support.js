// Danda's own support contact — not a business owner's number. Used
// wherever the app needs to reach the Danda team directly (e.g. the
// payment-verification failure banner in OwnerDashboard.jsx).
export const SUPPORT_WHATSAPP = '2349057285022';
